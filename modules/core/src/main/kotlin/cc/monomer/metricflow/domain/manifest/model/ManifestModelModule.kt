package cc.monomer.metricflow.domain.manifest.model

/**
 * Phase 2 scaffolding marker for the `:domain:manifest:model` module.
 *
 * The actual port from metricflow lands in Phase 3. This file exists so the
 * module compiles and can be referenced by other modules' tests.
 */
internal object ManifestModelModule {
    const val GRADLE_PATH: String = ":domain:manifest:model"
}
